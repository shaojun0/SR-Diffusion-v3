# SR-Diffusion v3 — test 分支 v2 像素目标训练报告（重大 Bug 修复）

> 日期: 2026-08-27 ｜ 分支: `test` ｜ 服务器: 2× RTX PRO 6000 (97GB), torch 2.12.1+cu130, transformers 5.15.1, datasets 5.0.1
> 本文档记录: 监督目标从「DINO patch 特征」改为「原始像素 pixel_values」后的完整训练 + 推理测试 + 重建可视化。

---

## 1. 背景: 为什么必须改目标（重大 Bug）

上一轮实验（`REPORT_test_v2_train_and_leak.md` §5.7 像素级逆向还原实验）已实证:

| 来源 | 像素还原 L1 (0-255) | 说明 |
|---|---|---|
| 真实 DINO 特征 → 像素（线性解码） | **9.8** | 特征里信息是有的 |
| 模型 F_hat（特征目标训练）→ 像素 | **64.9** | 退化为 ≈全图平均色(61) |
| 全图平均色参照 | 61.3 | 下限 |

**根因**: 工地图片大面积均匀，DINOv2 对 576 个 patch 输出的特征**空间变异性仅 ~5e-5**（每图近似常数向量）。监督"预测特征"时，模型学到"输出每图质心"即可拿到极低 L1（0.000038），**是假收敛**——特征空间 L1 无法反映真实还原能力，模型实际丢失了全部空间信息。

**修复方向（用户拍板）**: 最终要预测的是 **pixel_values**（原始像素），不是 feats。像素目标有真实空间结构（原图 std≈70-85），"输出质心"的损失压力（L1≈61）会迫使模型保留空间信息。

---

## 2. 代码改动（test 分支提交 `33fa0e3` / `98106c1` / `44baf09`）

### 2.1 model_v2.py — 新增 PixelHead，目标改为像素
- 新增 `PixelHead(dim → 14×14×3=588)`: 每 patch 特征解码回像素 patch。
- `SRPhase1V2.forward`:
  - 目标: `pixel_values` 归一化像素 → 拆成 (B,576,588) patch（row-major，与 DINO patch 布局一致）。
  - 每个采样步 t 的 `Y_t` 过 PixelHead → 像素 patch，平权全覆盖 L1: `loss = mean_t L1(Y_t_pix, target_pix)`。
  - `F_hat` = 采样步平均 → PixelHead → 像素预测 (B,N,588)；`recon` 为像素 L1 监控。
- **去掉加权体系**（用户要求）: `OutputQueryDecoder` 的 `loss_weight`/`step_weights`/`_build_weights` 整块删除，全部采样步平权。
- forward 加输入尺寸断言（14 的倍数 + patches 数匹配）。

### 2.2 train_v2.py — 沿用已定口径
- 去掉 `--decoder_loss_weight` 参数；默认 **fp32 全精度训练**（无 bf16/fp16 autocast）+ **batch_size 16/卡**（HF Trainer 风格，自上轮）。

### 2.3 infer_v2_test.py — 像素版推理测试
- 全量重建像素 L1（归一化空间 + 0-255 空间双口径）+ 每采样步渐进曲线（0-255）。

### 2.4 visualize_recon_pixel.py — 重建可视化
- 原图 vs 各采样步 t 像素重建（直接可视化像素，非 PCA 特征）。

---

## 3. 训练（fp32, 平权, bs16, 2 GPU DDP）

| 项 | 值 |
|---|---|
| 数据 | construction_site 工地 parquet: 7009 训练 + 3004 测试 |
| 预处理 | 1600×900 画布 → 448×252 模型输入（576 patches） |
| 架构 | DINOv2-large(不冻结) → ReEncoder(4层) → OutputQueryDecoder(平方采样 25 步) → PixelHead |
| 口径 | 全 fp32、平权 mean、bs16/卡×2、lr=1.5e-4 cosine、40 epochs / 8760 步 |
| 时长 | ~2h（1.30 it/s） |
| 产物 | `output/phase1_v2_pixel_fp32/final_model.pt`（1.47GB fp32）|

**训练 / eval 曲线**（eval 每 2000 步，归一化空间）:

| step | 2000 | 4000 | 6000 | 8000 | 8760 (final) |
|---|---|---|---|---|---|
| eval_loss | 0.5294 | 0.4735 | 0.4471 | 0.4236 | **0.4230** |

---

## 4. 推理测试（全量 3004 张 test, fp32, 0-255 像素空间）

### 4.1 全量重建

| 指标 | 数值 | 对照 |
|---|---|---|
| 全量重建像素 L1 (0-255) | **23.41 ± 7.64** | 全图平均色 ≈61（下限）→ **改善 2.6×** |
| 全量重建像素 L1 (归一化) | 0.4131 | — |

→ **重大修复成功**: 特征目标时代模型退化到"平均色"水平（L1=64.9），像素目标下模型真正重建了像素结构（L1=23.4），显著优于平均色 baseline。

### 4.2 渐进曲线（每采样步, 0-255 像素 L1）—— 前缀机制真实生效

```
t=   0 (前缀  1 键) L1 = 60.03   ← 仅 z_cls, 近似平均色(粗糙)
t=   1 (前缀  2 键) L1 = 22.93   ← 加 1 个 z_s 骤降到精细
t=   4..576        L1 = 22.7x    ← 平台
```

**关键转变**: 特征目标时代渐进曲线全平（所有步 L1≈0.0014，无差别）；像素目标下 **t=0 明显粗糙（60）、t≥1 精细（22.7）** —— KV 因果前缀语义在像素任务下真实可观测，且信息在 z_s[0] 就已充分（前缀 2 键≈全量）。

### 4.3 重建可视化（recon_visual.png）
原图 vs 各采样步（t=0/36/144/324/576）重建: 重建图保留整体色调与大体结构（std≈原图 70-85%），t≥1 即有清晰轮廓。数值佐证（3 张样本）:

| 图 | 原图 std | 重建 std | 原图边缘 | 重建边缘 | L1 |
|---|---|---|---|---|---|
| 0 | 66.8 | 57.3 | 32.0 | 10.5 | 32.2 |
| 1 | 78.3 | 70.5 | 13.6 | 5.4 | 16.2 |
| 2 | 85.2 | 81.2 | 20.2 | 6.8 | 14.8 |

重建有真实空间结构（std/边缘远大于 0），非平均色。

---

## 5. 结论

1. **重大 bug 已修复**: 监督目标从 DINO 特征改为原始像素后，模型从"输出质心假收敛"（像素 L1=64.9≈平均色）变为"真实像素重建"（L1=23.4，优于平均色 2.6×）。
2. **像素 L1 是还原能力的最终判据**: 特征空间 L1 因目标退化不可信；像素目标下所有指标（全量 L1、渐进曲线、可视化）一致、有意义。
3. **渐进机制恢复**: t=0 粗糙(60) / t≥1 精细(22.7)，KV 因果前缀语义真实生效。
4. **训练口径生效**: fp32 全精度 + 平权 mean + bs16，训练稳定收敛（eval 0.53→0.42）。

## 6. 已知限制 / 下一步建议

- 重建仍偏"平滑"（边缘强度约为原图 1/3）：PixelHead 是单层 Linear，可换小 CNN/上采样解码头恢复高频细节。
- t≥1 即达平台：可考虑减少采样步数加速（当前 25 步），或验证更短前缀的压缩能力。
- 可视化显示整体色调还原好、细节偏糊：后续可加感知损失（LPIPS）或多尺度 L1 提升锐度。

---

## 附: 相关提交（test 分支）

| commit | 内容 |
|---|---|
| `2655fc4` / `1a911ae` | 用户提交: 注意力机制改写 + 引文添加 |
| `d8354bb` … `010b57f` | 上一轮: 训练适配 / 泄露诊断 / 首份报告 |
| `17e5708` → `fc1602f` | 训练口径: uniform 平权 + 全 fp32 + bs16 |
| `3c438a5` / `8cd9ed3` / `09d74a4` | HF Trainer 重写 + transformers 5.15 适配 + eval 修复 |
| `2e86492` / `edb21fd` / `50f9e01` / `2d60cd8` / `01b7645` / `82a9693` | 常数判定 / 像素逆向实验 / 余弦评估（定位特征目标退化） |
| **`33fa0e3`** | **重大修复: 目标改为像素 (PixelHead) + 去掉加权体系** |
| `98106c1` | forward 输入尺寸断言修复 |
| `44baf09` | visualize_recon_pixel 重建可视化 |
