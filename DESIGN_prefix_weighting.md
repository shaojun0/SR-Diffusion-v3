# 设计草稿 — z_s 前缀加权机制（"随 k 增加，损失权重降低"）

> 状态: 草稿，待审核。作者: agent，需求来源: 用户 2026-08-25。
> 目的: 解决"还原精度 vs z_s 前缀长度 k"实验中暴露的问题——编码器信息摊匀、
> decoder 不会用小前缀，导致 k=1..32 还原精度平台期（无"预算/渐进"能力）。

## 1. 问题回顾（实验证据）

- k 扫描（3004 测试图，fp32 权重）: k=0 → 0.00407；k=1 → 0.00339；
  k=2..32 → ~0.0033（平台期，全量的 2.9×）；k=64..512 → 0.0032..0.0025；
  k=576（全量）→ 0.00114。
- 窗口探针（500 图，32-token 窗口）: z_s[0:32]=0.00329、z_s[64:96]=0.00328、
  …、z_s[384:416]=0.00326、z_s[512:544]=0.00342 —— **任意位置 32 个 token
  还原能力相同**，前段没有更多信息量 → 编码器把信息均匀摊在 576 个位置。
- 根因（两层）:
  1. decoder 全量训练，从未见过小前缀 → 不会用小前缀（可训练解决）;
  2. 编码器无"精华前置"动机（mask 只管 specials 间因果，它照常看全部
     patches，信息放哪个位置都行）→ 摊匀（需要训练压力才会变）。

## 2. 用户提议的机制

> 随着特殊 token（z_s）数量 k 的增加，给重建损失的权重降低：w(k) 递减。

即在训练中按前缀长度 k 给重建损失加权，让小 k 的重建"必须做得好"，
大 k（本来容易）放松要求。目标: 让模型学到"前段装粗结构、后段装细节"
的有序信息分布 + decoder 具备任意前缀还原能力（预算/渐进可用）。

## 3. 可行性评估：可行——但数学表述已修正（2026-08-26 经数值验证）

> ⚠️ 本版修正: 原"telescoping 恒等式"不成立（`verify_weighting_math.py` 数值反例
> 差 2.2e-2）。正确的恒等式与"前置"论证见下，完整推导见 `MATH_mask_analysis.md` §6。

记 L1_j = L1(前缀 j 重建)（j=0..N，L1_0 为仅 z_cls），Δ_i = L1_{i-1} - L1_i ≥ 0
（第 i 个 token 的边际改善），w_j 递减（w_0 ≥ w_1 ≥ … ≥ w_N ≥ 0）。

**正确恒等式 (I)**（数值验证差 6.9e-18）:

    Σ_j w_j·L1_j  =  (Σ_j w_j)·L1_N  +  Σ_i (Σ_{j<i} w_j)·Δ_i

即"前缀损失加权和"≡"全量项 + 各 token 边际贡献 Δ_i 以**累计权重 Σ_{j<i} w_j** 加权"
——注意该系数随 i **递增**，所以"w 递减 ⇒ 早段边际贡献权重更大"的旧解读**不成立**。

**真正的前置机制 = 梯度压力**（数值验证: w=1/(j+1) 时 Σ_{j≥i}w_j: i=1→5.94,
i=N/2→0.69, i=N→0.0017）:

    ∂/∂z_s[i] Σ_j w_j·L1_j  =  Σ_{j≥i} w_j · ∂L1_j/∂z_s[i]

token i 参与所有 j≥i 的前缀重建，其总梯度压力 ∝ **Σ_{j≥i} w_j，随 i 递减**
⇒ 越早的 token 收到越大压力 ⇒ 信息前置 ✓（实践结论不变，理由从"边际系数"改为"梯度压力"）。

**天然加成（精确成立）**: 若 k 在 [1,N] 均匀采样（w_j≡1），位置 i 的压力
∝ N-i+1（i=1→576, i=N→1，线性递减）——均匀采样本身就产生"前面重要"的偏置。

**若真要"边际贡献加权"语义**（早段边际权重更大）: 目标应取 Σ_i w_i·Δ_i（= w_1 L1_0 − w_N L1_N
+ Σ_{j<N}(w_{j+1}−w_j)L1_j，恒等式 (II)，数值验证差 1.1e-19）——它是"奖励小前缀做得好"的
差分形式，实现上等价于对"相邻 k 的损失差"加权，比 Σ_j w_j L1_j 复杂，默认不采用。

**两个必要通道同时被激活**:
- decoder 端: 训练中见到各种 k → 学会用小前缀（当前完全不会）;
- 编码器端: 小 k 重建差 → 高损失 → 梯度逼编码器把可还原的全局信息
  放进靠前的位置（它看得到全部 patches，有能力重新组织）。

## 4. 待拍板的设计选项（明天你细化）

| 选项 | 候选 | 权衡 |
|---|---|---|
| k 采样分布 | (a) uniform [k_min..N]；(b) log-uniform（小 k 更密）；(c) 混合：p_full 比例全量 + (1-p_full) 随机前缀 | uniform 最简单且自带线性偏置；混合比例控制全量质量不退化 |
| 权重形状 | (a) w(k)=1/(k+1)；(b) (N/k)^p, p∈[0.5,1]；(c) 带地板: max(w_min, 1/(k+1)) | 折进采样概率则无需再乘权重（重要度采样）；地板防止全量分支权重趋零 |
| k 下限 | k_min ∈ {4, 8, 16} | 防 k 过小损失爆炸/梯度不稳 |
| 全量保底 | 每步 p_full≈0.5~0.7 概率仍用全量 k=N | 保住主用途（全量还原精度） |
| 调度 | 固定 vs 退火（前期全量为主，后期加大前缀比例） | 退火更稳，代价是多一个超参 |

## 5. 与已删 SelectHead/预算机制的关系

git 历史中的 SelectHead/边界分布/STE/率惩罚是**学习式门控 + 预算约束**
（动态决定留哪些 token）；本方案是**静态调度式权重**（按 k 给损失加权，
不学门控、无 STE）。二者是同一目标（重要性分配）的两端：
- 本方案: 熵更低（一个权重函数 + 采样），先验证"有序信息分布能否被
  训练出来"；
- SelectHead: 更灵活（逐 token 学习重要性），但复杂度/不稳定风险高。
建议先做本方案（YAGNI: 先不加实体），若有序性仍不足再考虑恢复门控。

## 6. 落地改动点（实现量很小）

- `train_v2.py`: 新增 `--prefix_curriculum`（默认关）:
  ```python
  # 伪代码（明天细化）
  if args.prefix_curriculum and random() < p_mix:
      k = sample_k(k_min, N, dist)          # 采样前缀长度
      z_cls, z_s, patch = model._encode_z(x)
      F_hat = model.decoder(z_cls, z_s[:, :k])
      loss = w(k) * L1(F_hat, patch)         # w 递减; 或折进采样概率
  else:
      loss = model(x)["loss"]                # 原全量路径
  ```
- `model_v2.py`: 无需改（FeatureDecoder 已支持 k<N 前缀，掩码/输出已验证）。
- `infer_k_sweep.py` + 窗口探针: 复用，作训练后验证。
- 文字分支的 `z_keep` 条件自动受益（同一编码器），无需额外改动。

## 7. 验证方案（训练后必须重测）

1. `python infer_k_sweep.py`（k=0..32+锚点+全量）: 预期 k=1..32 的
   L1 显著下降（向全量靠拢），平台期消失或大幅缓解；
2. 窗口探针（32-token 窗口扫描）: 预期前段窗口信息量 > 后段（有序化
   出现），即 z_s[0:32] 的还原明显好于 z_s[384:416]；
3. 监控全量 k=N 的 L1: 允许小幅退化（如 ≤1.5× 当前 0.0011），
   过度退化说明 w 地板/混合比例没调好。

## 8. 风险与缓解

| 风险 | 缓解 |
|---|---|
| 全量还原精度退化 | p_full 保底批次 + w(k) 地板；监控 k=N L1 |
| 小 k 训练不稳（损失大） | k_min 下限 + 已有 grad clip |
| 训练时间增加 | 每步最多多一次 decoder 前向（4 层小网络），可忽略 |
| 有序性仍不够 | 升级到 SelectHead 式学习门控（见 §5） |
| "只算前 k 的预算推理"（截断编码器）时 z_s[:k] 与全量不一致 | 掩码只保证**直接路径**前缀因果；深度≥2 时 patches 全局枢纽使传递闭包成完全图（回渗），详见 `MATH_mask_analysis.md` §3.2。k-sweep（只截 decoder）不受影响；截断编码器的推理需先验证或接受近似 |

## 9. 参考数据（2026-08-25 实测）

- k 扫描结果: `output/phase1_v2/k_sweep_full.json`（fp32 final_model）
- final_model: `output/phase1_v2/final_model.pt`（fp32，从 ckpt-16000 重导出）
- 注意: 训练日志 eval 为 bf16 损失略低估；fp32 全量测试 L1 ≈ 0.00114
- 数学分析: `MATH_mask_analysis.md`（掩码必要不充分论证、恒等式修正、梯度压力机制）
